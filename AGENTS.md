# Spatial Interface development

The repository is Spatial-Interface; distribution name spatial-interface;
Python namespace spatial_interface. Use relative project paths in public docs.
Keep data, environments, credentials and full experiment outputs outside Git.

CC is the preferred implementation and environment executor when available;
the coordinating agent verifies results. Use the project's dedicated context,
Opus 5 with high effort. Preserve a checkpoint and clear context before assigning
new work when context exceeds 50%. If CC is unavailable, the coordinator may
finish the authorized work. Preserve existing work and other running services.

Default experiments use JKWL Responses, gpt-6-astra / medium, native Codex 0.153.4,
direct_geometry with grounded feedback. Do not silently switch providers.
The robot MCP must initialize before model inference. Exercise the real native
look/end path when validating client changes.

Use scripts/methods.py and the experiment CLI to record exact method/client/Git
identity. Changes to tools, controller, feedback or guides require a new release.
Never modify the original frozen VIA records to match this repository. Preserve
CLAUDE.md and the method guides' robot evaluation restrictions.

Run requested routine, reversible work without repeated confirmation. Running an
experiment authorizes reasonably expected preparation and verification; preserve
explicit model, budget and protocol constraints. Use fresh output directories.
Never stop an unknown process just because it occupies a selected port.

Do not launch full benchmarks merely to check setup. For experiment launch
requests, report when the first episode is confirmed active, with its ETA,
unless the user explicitly requests waiting for results. Prefer ETA handoff over
unsolicited recurring monitors. Keep public documentation free of machine-local
paths and credentials.
